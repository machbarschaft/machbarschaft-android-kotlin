package jetzt.machbarschaft.android.service

/**
 * Constants representing the remote server API URLs.
 */
object ApiConstants {

    //test base url
    val BASE_URL = "https://jsonplaceholder.typicode.com"

    object TestApi {
        const val GET_ALL = "/users"
    }
}